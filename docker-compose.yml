version: '3.8'

services:
  server:
    build: ./
    ports:
      - '80:3000'
    environment:
      DATABASE_HOST: 3.101.22.86
      DB_PASS: password
      MYSQL_DB: sdcqa
      MYSQL_USER: root
      MYSQL_PORT: 3306
    restart: on-failure
